<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{{title}}</title>
        <style>
            header {
                background-color: #000; 
                padding: 20px;
                text-align: center; 
                border-bottom: 2px solid #444;
            }

            header nav ul {
                list-style: none;
                padding: 0;
                margin: 0;
                display: flex;
                justify-content: center; 
                gap: 30px; 
            }

            header nav ul li {
                display: inline; 
            }

            header nav ul li a {
                color: #fff; 
                text-decoration: none; 
                font-size: 1.2rem; 
                font-weight: bold; 
            }

            header nav ul li a:hover {
                color: #FFA500; 
            }
        </style>
    </head>
    <body>
        <header>
            <nav>
                <ul>
                    {{#if user}}
                        {{#if (eq user.role 'admin')}}
                            <li><a href="/admin/products">📋Gestor</a></li>
                        {{/if}}
                        {{#if (eq user.role 'user')}}
                            <li><a href="/products">🍣Productos</a></li>
                            <li><a href="/carts/{{user.cart}}">🛒 Carrito</a></li>
                        {{/if}}
                        <li><a href="/user">👤Usuario ({{user.first_name}})</a></li>
                        <li><a href="/api/sessions/logout">🚪Logout</a></li>
                    {{else}}
                        <li><a href="/api/sessions/login">💻Login</a></li>
                    {{/if}}               
                </ul>
            </nav>
        </header>
        {{{body}}}
    </body>
</html>