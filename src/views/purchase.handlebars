<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="UTF-8">

    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles del Ticket de Compra</title>
    <style>
        body {
            background-color: #000;
            color: white;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #333;
            padding: 20px;
            text-align: center;
            border-bottom: 2px solid #444;
        }

        header h1 {
            color: #ffa500;
            margin: 0;
        }

        .ticket-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 90%;
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            background-color: #222;
            border-radius: 5px;
            border: 1px solid #444;
        }

        .ticket-container h2 {
            font-size: 1.8rem;
            color: #ffa500;
            margin-bottom: 20px;
        }

        .ticket-details p {
            font-size: 1rem;
            margin: 5px 0;
        }

        .ticket-details strong {
            color: #ffa500;
        }

        .back-button {
            background-color: #ff4500;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            margin-top: 20px;
        }

        .back-button:hover {
            background-color: #ff6347;
        }
    </style>
</head>

<body>

    <div class="ticket-container">
        <h2>Detalles del Ticket de Compra</h2>

        <div class="ticket-details">
            <p><strong>Código:</strong> {{ticket.code}}</p>
            <p><strong>Fecha de Compra:</strong> {{ticket.purchase_datetime}}</p>
            <p><strong>Monto Total:</strong> ${{ticket.amount}}</p>
            <p><strong>Comprador:</strong> {{ticket.purchaser}}</p>
        </div>

        <button class="back-button" onclick="window.location.href='/products'">Volver a Productos</button>
    </div>
</body>

</html>